/**
 * Publiczne, stabilne adresy. Nie wpisuj tu numeru wydania - backend przekieruje
 * pod aktualny plik instalatora ustawiony w DESKTOP_DOWNLOAD_URL.
 */
export const downloads = {
  windowsX64: "https://api.paczkowo.net/downloads/latest/windows-x64",
} as const;
